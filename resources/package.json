{
  "name": "spigetScraperResources",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:pluget/spigetScraper.git",
  "author": "mbledkowski <pub@mble.dk>",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "serve": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/jsonfile": "^6.1.0",
    "@types/node": "^18.0.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "jsonfile": "^6.1.0"
  }
}
